
const PATH = {
    index: "/",
    chats: "/chats",
    currentChat: "/chats/:chatId",
    users: "/users",
    userProfile: "/users/:userName",
    login: "/login",
    register: "/register",
}

export default PATH;